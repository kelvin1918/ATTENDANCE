# ─────────────────────────────────────────────────────────────────────────────
# BatStateU Attendance System — Environment Variables
# ─────────────────────────────────────────────────────────────────────────────
# HOW TO USE:
#   1. Copy this file and rename it to exactly:  .env
#   2. Fill in your real credentials below
#   3. NEVER commit .env to GitHub — add it to .gitignore
#
# For Render.com deployment:
#   Go to your Render service → Environment tab → add each variable there.
#   Do NOT upload the .env file to Render — use their dashboard.
# ─────────────────────────────────────────────────────────────────────────────

# ── NEON POSTGRESQL (required) ────────────────────────────────────────────────
DB_HOST=ep-cool-darkness-ao4fnwdj-pooler.c-2.ap-southeast-1.aws.neon.tech
DB_PORT=5432
DB_NAME=neondb
DB_USER=neondb_owner
DB_PASSWORD=npg_WI7TMFYw0vOe

# ── CLOUDINARY (required for signature display on Render + PDF) ───────────────
CLOUDINARY_CLOUD_NAME=dkojbt2lr
CLOUDINARY_API_KEY=386199447565477
CLOUDINARY_API_SECRET=ax-BAga059T1oj08pfTk7hlzRCs

# ── OPTIONAL: SMTP EMAIL (for attendance notifications) ───────────────────────
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_gmail@gmail.com
# SMTP_PASS=your_google_app_password
